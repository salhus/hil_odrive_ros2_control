yaml name=config/controllers.yaml
controller_manager:
  ros__parameters:
    update_rate: 200  # Hz

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    motor_effort_controller:
      type: effort_controllers/JointGroupEffortController

motor_effort_controller:
  ros__parameters:
    joints:
      - motor_joint
